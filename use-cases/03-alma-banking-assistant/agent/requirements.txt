# Pinned to the versions validated locally + smoke-tested against AgentCore Memory.
strands-agents==1.0.1
strands-agents-tools==0.2.1
bedrock-agentcore==1.4.0
# ADOT distro enables AgentCore Runtime GenAI observability
# (traces, spans, logs → CloudWatch). Required when using
# `opentelemetry-instrument` as the container entrypoint.
aws-opentelemetry-distro>=0.10.0
boto3
